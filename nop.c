#include "monty.h"
/**
 * nop - does nothing
 * @head: pointer to stack_t list
 * @line_number: line command is on in file
 *
 * Return: always void
 */
void nop(stack_t **head, unsigned int line_number)
{
	if (*head != NULL && line_number > 0)
	{
	}
}
